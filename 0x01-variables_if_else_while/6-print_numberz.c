#include <stdio.h>

/**
 * main - my project entry point
 *
 * Return: returns 0 when everything works well
 *
*/

int main(void)
{
	int l;

	for (l = 48; l < 58; ++l)
	{
		putchar(l);
	}
	putchar(10);
	return (0);
}
